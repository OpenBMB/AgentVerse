// Internal events

const RemoveWaitEvents = '_remove.wait';
const StopPlayEvent = '_remove.play';

const ClearEvents = [
    RemoveWaitEvents,
    StopPlayEvent
]

export {
    RemoveWaitEvents,
    StopPlayEvent,
    ClearEvents
}